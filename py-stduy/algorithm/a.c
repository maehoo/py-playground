#include <stdio.h>
#include <string.h>
int main(void)
{
    int N.M;
    scanf("%d%d",&N,&M);
    char * arr1[N];
    char * arr2[M];
    char str[500];
    int i;
    for(i=0;i<N;i++)
    {
        scanf("%s",str);
        arr[i] =  
        
    }
    for(i=0;i<M;i++)
    {
        scanf("%s",&str[i])
    }
}